const mysql = require("mysql");

const createConnection = mysql.createConnection(
    {
        host: "localhost",
        user: "shant",
        password: "",
        database: "phpmyadmin/db_structure.php?server=1&db=mycrude"
    }
);

createConnection.connect((err) => {
    if (err) {
        console.log("erros ur getting")
    }
    else {
        console.log("yes connected")
    }
})